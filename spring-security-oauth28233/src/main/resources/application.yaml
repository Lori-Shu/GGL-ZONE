server:
  port: 8233
  servlet:
    session:
      cookie:
        name: OAUTH2SESSION

#spring:
#  redis:
#    host: localhost
#    port: 6379
#    lettuce:
#      pool:
#        max-active: 20
#        max-wait: -1
#        max-idle: 5
#        min-idle: 0
spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  application:
    name: oauth-client

