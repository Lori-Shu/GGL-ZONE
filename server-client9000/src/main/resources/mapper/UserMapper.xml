<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ggl.cloud.mapper.UserMapper">
<select id="getRegistryCount" resultType="int">
 select count(*) from user where Date(create_time)=#{date}  and deleted = 0;
</select>
</mapper>